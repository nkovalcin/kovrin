[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/uvicorn kovrin.api.server:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
