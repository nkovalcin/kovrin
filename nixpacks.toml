[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["python3 -m venv /opt/venv"]

[phases.build]
cmds = ["/opt/venv/bin/pip install '.[api,superwork]'"]

[start]
cmd = "/opt/venv/bin/uvicorn kovrin.api.server:app --host 0.0.0.0 --port ${PORT:-8000}"
